require "spec_helper"

RSpec.describe TimeWrapper do
  it "has a version number" do
    expect(TimeWrapper::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
